<%= form_for(tweet) do |f| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--Add bootstrap formatting, form-group  and form-control#MDM-->
  <!-- By removing the following code, you actually remove teh need to specify a user_id when creating a new tweet #MDM
  <div class="form-group">
    <%= f.label :user_id %>
    <%= f.text_field :user_id, class: "form-control" %>
  </div>
  -->

  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: "form-control" %>
  </div>

  <!--<div class="actions">-->
    <%= f.submit class: "btn btn-primary" %>
  <!--</div>-->
<% end %>
